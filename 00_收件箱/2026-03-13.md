# 2026-03-13 Friday 工作记录

## 💼 工作
- [x] 继续测试“今天吃什么”小程序，争取明天上线，并开始发小红书笔记（已完成自动生成笔记流程，由于小程序认证还没有通过，待通过后，再开始发笔记，记得下周一提醒我）
- [x] 完成“电脑自动录屏 + 自动生成小红书内容”的 skill，跑通并发第一篇笔记
- [x] 与潘贤双沟通下周末去环境监测总站部署
- [x] 与刘文达沟通项目具体情况：影像是否要购买、前期沟通中对精度有没有要求、项目能够拿出多少经费（平台和模型用他们的） 已沟通，总体来考虑可能至少要收600万才行，不含影像，不含软件，下周一跟他最终确定
- [x] 开始设计景泰项目能够先做的内容，比如二三维一体化地图控件、720 全景照片查看控件（基本完成720全景照片查看控件的设计方案和实施方案，先完成这个，后续再做二三维一体化地图控件）
- [x] 把“每天自动推送 AI 新闻”这个 skill 新建并调试好

## 🏠 生活
- [x] 帮女儿存钱，同时放到投顾产品里
- [x] 取洗好的羽绒服

## 📋 补充说明
- 明天的重点是：先把能推进上线和能尽快产出内容的事往前排。
- 涉及沟通的事项，优先把要问的问题列清楚，避免来回沟通。
- 生活事项也单独列出来，避免被工作任务挤掉。

---

## ✍️ 过程记录
### 上午
- 继续推进“电脑自动录屏 + 自动生成小红书内容”的整条链路，先定位并确认项目目录为 `flows/xhs-daily`。
- 跑通了 ReelLingo 录屏流程，确认当前稳定链路应走 `3456` 端口的 CDP Proxy，而不是直接改用 BrowserWing `8080`。
- 补了前置检查脚本：运行前自动检查 / 拉起 `bw-chrome` 容器和 `cdp-proxy`，避免每次手动确认环境。
- 把运行参数从一长串手动 `export` 改成了长期可维护方案：本地 env 文件 + 一键运行脚本。
- 新建了 `run_reellingo_once.sh`，现在可以一条命令完成第一步录屏。
- 梳理并补全了 `flows/xhs-daily` 的 README，把“第一步怎么跑”写清楚，方便后续查询。
- 设计并落地了第二步 `post_plan.json` 的中等完整版本 schema，同时整理了第二步说明文档和主 Prompt。
- 新建了 `xhs-reellingo-pipeline` skill，把“录一个视频 + 生成一篇小红书笔记”串成可复用流程，后面可以直接给 cron 调用。
- 给第二步接入了 humanizer 思路，让文案先生成，再做人话化收紧，减少 AI 腔和宣传腔。
- 优化第一步：在录屏前提取视频本身的标题和简介，并写入 `collector_output.json`，给第二步提供更具体的写作依据。
- 调整第二步逻辑：不再先做机械的场景分类，而是直接围绕视频标题和简介本身写文案，避免误判。
- 以“子女入学面谈”为例重跑了全流程，验证标题/简介提取已能抓到视频本身内容，且第二步可直接围绕该内容生成更贴题的小红书成稿。

### 下午
- 明确了“每天自动推送 AI 新闻”这条需求不只是新闻聚合，而是偏内容选题视角的 AI 应用情报雷达：重点关注 AI 工具、AI 编程、Agent / OpenClaw、生图生视频、效率工具等应用层动态，不看论文和研究。
- 把这个新能力正式命名为 `ai-content-radar`，并把输出结构从普通资讯汇总升级成三层：新闻层、讨论层、生态信号层，再附“本期最适合做内容的 5 个选题”和趋势小结。
- 完成了 skill 第一版骨架与参考文档设计，补齐了评分规则、信源策略、输出模板、运行手册、查询模板、去重聚类规则、来源抓取 recipe 等内容。
- 明确了 Agent Reach 在这件事里的角色是“渠道接入层”，不是统一搜索器；后续应基于已接好的 X、GitHub、RSS、网页等上游能力来做聚合与评分。
- 先后跑了多轮试跑样例，验证了 X 适合抓讨论层、网页 / RSS 适合抓新闻层、GitHub 适合抓生态层；同时确认 OpenClaw 周边安装器、管理台、状态页、多实例与 skill 生态已经形成明显的应用层信号。
- 完成了 `gh auth login` 配置，正式把 GitHub 接入这条 skill 的可用信源体系，用于补强 AI 编程工具、Agent 工作流与开源生态的趋势判断。
- 在实战打磨后，完成 `ai-content-radar` skill 打包，生成 `/root/.openclaw/workspace/dist/ai-content-radar.skill`，说明这条“每天自动推送 AI 新闻”的 skill 已完成新建、调试与第一版封装。

### 晚上
- 

---

## ✅ 今日完成
- [x] 完成“电脑自动录屏 + 自动生成小红书内容”的 skill 基础搭建，并跑通从录屏到生成 `post_plan.json` 的全流程。
- [x] 跑通 ReelLingo 第一阶段录屏链路，确认使用 CDP Proxy（3456）+ `bw-chrome` 容器的方式更稳定。
- [x] 新增前置检查脚本和一键运行脚本，减少手动准备环境的步骤。
- [x] 完成第二步文案 schema、Prompt、说明文档与可读成稿输出结构。
- [x] 新建 `xhs-reellingo-pipeline` skill，支持后续 cron 直接调用第一步 + 第二步。
- [x] 完成文案 humanizer 接入，让第二步输出更像人话。
- [x] 完成第一步对视频标题和简介的提取，并让第二步能直接基于标题和简介写文案。
- [x] 已经完成了小红书视频和笔记飞书链路的打通，可以发送视频和笔记内容了。
- [x] 完成 `ai-content-radar` skill 的新建、试跑、规则打磨与打包，形成“新闻层 / 讨论层 / 生态信号层”的 AI 应用情报雷达。

## 📚 今日收获
- 录屏链路里，环境正确比“看起来差不多的参数”更重要；这次确认了 `3456` 是 CDP Proxy，对最终录制结果影响很大。
- 第二步写小红书文案时，直接围绕视频标题和简介来写，比先做一层固定场景分类更稳，也更不容易跑偏。
- 把流程拆成“env 配置 / 前置检查 / 运行入口 / skill / schema”几层后，后面接 cron 会轻松很多。

## 🔥 明日重点
### 工作
- [ ] 

### 生活
- [ ] 

## 💭 随记
- 这条“自动录屏 + 自动生成小红书内容”的链路今天已经从零散调试推进到可复用流程。
